import { readFileSync, writeFileSync } from 'fs';
import { resolve } from 'path';

const ROOT = new URL('..', import.meta.url).pathname.replace(/^\/([A-Za-z]:)/, '$1');

function patchFile(relPath, replacements) {
  const full = resolve(ROOT, relPath);
  let src = readFileSync(full, 'utf8');
  const original = src;
  for (const [from, to] of replacements) {
    if (!src.includes(from)) {
      console.warn(`[MISS] "${from.slice(0, 50).replace(/\r?\n/g,'↵')}…" — ${relPath}`);
      continue;
    }
    src = src.split(from).join(to);
  }
  if (src !== original) {
    writeFileSync(full, src, 'utf8');
    console.log(`[OK]  ${relPath}`);
  } else {
    console.log(`[SKIP] ${relPath} (no changes)`);
  }
}

// ──────────────────────────────────────────────
// 1. peeling-not-filler-myth.md
// ──────────────────────────────────────────────
patchFile('src/content/blog/myth/peeling-not-filler-myth.md', [
  // 1-A: 제목
  [
    '\u5FC5\u7075\u300D\uC774\uC5D0\uC694 \u300C\uD544\uB7EC\u300D\uC608\uC694? \u2014 \uD55C \uAE00\uC790 \uCC28\uC774, \uC2DC\uC220\uC740 \uC6B0\uC8FC\uAC00 \uB2E4\uB984"',
    '\u5FC5\u7075\u300D\uC774\uC5D0\uC694 \u300C\uD544\uB7EC\u300D\uC608\uC694? \u2014 \uD55C \uAE00\uC790 \uCC28\uC774, \uD558\uB294 \uC77C\uC740 \uC644\uC804\uD788 \uB2E4\uB985\uB2C8\uB2E4"',
  ],
  // 1-A (UTF-8 plain fallback)
  [
    '「필링」이에요 「필러」예요? — 한 글자 차이, 시술은 우주가 다름"',
    '「필링」이에요 「필러」예요? — 한 글자 차이, 하는 일은 완전히 다릅니다"',
  ],
  // 1-B: 탭 A/B → 필링/필러 직접 표기
  [
    '**두 개의 탭**이 동시에 뜹니다.',
    '두 가지 방향으로 나뉩니다.',
  ],
  [
    '**탭 A: 필링** — 각질·표면을 정리하고 피부를 \u2018새로 고치게\u2019 만드는 쪽.',
    '필링 — 각질·표면을 정리하고 피부를 \u2018새로 고치게\u2019 만드는 쪽.',
  ],
  [
    "**탭 A: 필링** — 각질·표면을 정리하고 피부를 '새로 고치게' 만드는 쪽.",
    "필링 — 각질·표면을 정리하고 피부를 '새로 고치게' 만드는 쪽.",
  ],
  [
    '**탭 B: 필러** — 꺼진 곳·주름 축을 **채워 넣는** 쪽.',
    '필러 — 꺼진 곳·주름 축을 채워 넣는 쪽.',
  ],
  // 1-C: 가르면 → 구분하면 (이미 적용됐으면 MISS 처리됨)
  [
    '받침과 느낌 — 가르면, 하는 일이',
    '받침과 느낌 — 구분하면, 하는 일이',
  ],
  // 1-D: 상담의 플롯
  [
    '상담의 플롯이 반은 정해집니다.',
    '상담 방향의 절반은 결정됩니다.',
  ],
]);

// ──────────────────────────────────────────────
// 2. botox-stop-responding-myth.md
// ──────────────────────────────────────────────
patchFile('src/content/blog/myth/botox-stop-responding-myth.md', [
  // 2-A: 소제목
  [
    '들려오는 건 한 문장인데, 머릿속 영화는 이미 풀버전',
    '들려오는 건 한 문장인데, 속뜻은 여러 가지일 때',
  ],
  // 2-B: 시나리오 자동 재생
  [
    '솔직히 시나리오가 자동 재생됩니다. 항체가 생겼다, 몸이 약을 알아챈다, 브랜드를 바꿔야 한다\u2026 드라마와 검색창이 똑같은 자막을 달아 준 기분이에요.',
    '솔직히 머릿속에서 하나의 결론으로 달려가기 쉽습니다. 항체가 생겼다, 몸이 약을 알아챈다, 브랜드를 바꿔야 한다\u2026 검색창이 같은 답만 돌려주는 기분이에요.',
  ],
  // 2-C: 공포 영화
  [
    '한 가지 공포 영화로만 기억해 버리는 건 조금 억울한 오해입니다.',
    '한 가지 걱정으로만 기억해 버리는 건 조금 억울한 오해입니다.',
  ],
  // 2-D: 플롯 반전 소제목 (curly single quotes U+2018/U+2019)
  [
    '## 플롯 반전: 범인이 꼭 \u2018내성\u2019만은 아닐 때',
    "## \u2018내성\u2019 말고 다른 원인을 먼저 살펴볼 때",
  ],
  // straight-quote fallback
  [
    "## 플롯 반전: 범인이 꼭 '내성'만은 아닐 때",
    "## '내성' 말고 다른 원인을 먼저 살펴볼 때",
  ],
  // 2-E: 스포일러
  [
    '여기서 스포일러를 하나 던질게요.',
    '먼저 짚어 드릴 내용이 있습니다.',
  ],
  // 2-F: 처방표 → 용량·부위 (이미 적용됐으면 MISS)
  [
    '예전 처방표를 그대로 기대하는 순간',
    '이전 용량·부위를 그대로 기대하는 순간',
  ],
  // 2-G: 놀라움이 보너스 (이미 적용됐으면 MISS)
  [
    '첫 시술은 놀라움이 보너스예요.',
    '첫 시술은 변화 자체가 새롭게 느껴지는 시기입니다.',
  ],
  // 2-H: 한 권짜리 소설 (curly double quotes)
  [
    '\u201c안 먹는다\u201d는 말은 종종 한 권짜리 소설인데, 우리는 표지만 보고 결말을 추측하고 있는 셈이에요.',
    '\u201c안 먹는다\u201d는 말은 여러 맥락이 겹쳐 있는데, 하나의 단어로만 결론 내리고 있는 셈입니다.',
  ],
  // 2-I: 소제목 (이미 적용됐으면 MISS)
  [
    '## 오늘의 책갈피 세 장',
    '## 상담 전 챙겨 갈 세 가지 질문',
  ],
  // 2-J: 책갈피 (이미 적용됐으면 MISS)
  [
    '가볍게 책갈피만 꽂아 두고 가도 대화가 빨라집니다.',
    '가볍게 정리해 두고 가도 대화가 빨라집니다.',
  ],
  // 2-K: 같은 책을 펼친 (이미 적용됐으면 MISS)
  [
    '같은 책을 펼친 상태에서 시작할 수 있어요.',
    '같은 맥락에서 이야기를 시작할 수 있어요.',
  ],
  // 2-L: 소제목 (이미 적용됐으면 MISS)
  [
    '## 마지막 장을 덮기 전에',
    '## 마무리하며',
  ],
  // 2-M: 스포일러는 없고 (이미 적용됐으면 MISS)
  [
    '스포일러는 없고, 다음 상담 때 가져가기 좋은 질문만 조금 늘어납니다.',
    '어렵지 않고, 다음 상담 때 가져가기 좋은 질문만 조금 늘어납니다.',
  ],
]);

console.log('\n완료.');
